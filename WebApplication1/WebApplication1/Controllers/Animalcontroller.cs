using Microsoft.AspNetCore.Mvc;
using System.Runtime.CompilerServices;

namespace WebApplication1.Controllers
{
    public class Animalcontroller
    {
        using SqlConnection con = newSQLConnection()
        using SqlCommand con = newSqlCommand();
        con.Connection = ConcurrentExclusiveSchedulerPair;

        con.Open();
        SqlDataReader reader = ConcurrentExclusiveSchedulerPair.ExecuteReader()
        List<Animal> animals = new List<Animal>();
        while 
    }
{
    public class AnimalsController : ControllerBase
{
    private IAnimalServise _animalService;
    public AnimalsController(IAnmalController _animalServices)
}
}
}
